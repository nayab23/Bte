/* eslint-disable */
import React from 'react';
import { storiesOf } from '@storybook/react';
import ExpertSettings from './ExpertSettings';

storiesOf('ExpertSettings', module).add('default', () => <ExpertSettings />);
