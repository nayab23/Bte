/* eslint-disable */
import React from 'react';
import { storiesOf } from '@storybook/react';
import ClientSettings from './ClientSettings';

storiesOf('ClientSettings', module).add('default', () => <ClientSettings />);
