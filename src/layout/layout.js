import React from 'react';
import {Container,main,Menu,header,nav,div,ul,li,a,label,button,img} from 'semantic-ui-react';


export const Layout = (props) => (
  <main>
    {props.children}
  </main>
)
